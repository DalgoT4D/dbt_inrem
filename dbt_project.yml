
name: 'dbt_inrem'
version: '1.0.0'
config-version: 2
profile: 'dbt_inrem'
model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
